/****** FORMATTING BASED ON http://stackoverflow.com/questions/20922755/print-heap-array-in-tree-format ******/
class heapFormatter {
public:
	size_t IntegerLogarithm2(size_t arg);
	size_t IntegerPower2(size_t arg);
	size_t LineLength(size_t level, size_t item_width, size_t spaces_between);
	int formatHeap(int input [], int size);
};